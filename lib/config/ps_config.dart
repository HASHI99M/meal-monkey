class PsConfig{
  PsConfig._();
  static const ps_default_font_family = 'Metropolis-Medium';

  static const Duration animation_duration = Duration(milliseconds: 500);

  static const Duration default_duration = Duration(milliseconds: 2200);
}